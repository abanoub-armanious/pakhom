% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structured_schemas.R
\name{.validate_schema}
\alias{.validate_schema}
\title{Lightweight check that a list looks like a valid JSON Schema for our use}
\usage{
.validate_schema(schema, path = "$")
}
\arguments{
\item{schema}{A schema list (e.g., the output of \code{.coding_schema()}).}

\item{path}{Internal: schema path, used for error messages.}
}
\value{
TRUE invisibly if the schema is well-formed; otherwise stops
with a descriptive error pointing at the violating subschema.
}
\description{
Not a full JSON Schema validator -- just enough to catch the OpenAI
strict-mode pitfalls at package-load / test time so a malformed schema
is caught before it hits the API. Specifically:
\itemize{
\item Top-level must be an object schema with type = "object".
\item Every object must have additionalProperties = FALSE.
\item Every object's \code{required} must list every key in \code{properties}.
\item Required and enum arrays must be lists (not character vectors)
to avoid jsonlite auto_unbox collapsing single-element arrays.
}
}
\keyword{internal}
