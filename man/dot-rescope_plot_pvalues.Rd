% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/14_correlations.R
\name{.rescope_plot_pvalues}
\alias{.rescope_plot_pvalues}
\title{Overlay re-scoped (family-corrected) adjusted p-values onto the plot p-matrix}
\usage{
.rescope_plot_pvalues(pa, var_names, rescoped_df)
}
\arguments{
\item{pa}{Adjusted-p matrix aligned to the correlation matrix (original names).}

\item{var_names}{Character vector of the matrix's ORIGINAL variable names.}

\item{rescoped_df}{The \code{\link[=extract_significant]{extract_significant()}} data frame (var1, var2, p_value).}
}
\value{
The p-matrix with kept pairs' re-scoped adjusted p overlaid.
}
\description{
extract_significant re-scopes the BH/Bonferroni multiple-comparison family to
the non-excluded (substantive) pairs (M2). The plot otherwise reads the
full-matrix \code{results$p_adjusted}, so a kept pair near the threshold could
read n.s. on the heatmap/lollipop while the report TABLE (which uses the
re-scoped df) calls it significant. This overlays the df's re-scoped adjusted
p (\code{p_value}) onto the plot matrix for kept pairs, keeping figure and
table consistent. Excluded pairs are left untouched (blanked separately via
\code{\link[=.mask_excluded_pvalues]{.mask_excluded_pvalues()}}). A NULL / schema-incomplete df leaves the matrix
unchanged (byte-identical, e.g. when nothing was excluded).
}
\keyword{internal}
