% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/09_coding.R
\name{.handle_one_coded_segment}
\alias{.handle_one_coded_segment}
\title{Process one coded segment from either the schema or citations path}
\usage{
.handle_one_coded_segment(
  seg,
  seg_index,
  use_citations,
  use_framework = FALSE,
  framework_spec = NULL,
  ai_meta,
  documents,
  text,
  entry_id,
  state,
  acc,
  audit_log,
  fabrication_log
)
}
\value{
Updated \code{state} (immutable per-call; the codebook is
updated when a non-fabricated segment is processed).
}
\description{
Builds a path-appropriate \code{QuoteProvenance} (free-form via
\code{make_quote} or citation-bridged via \code{make_quote_from_citation}),
runs the verification ladder, and -- if not fabricated -- updates the
codebook and accumulators.
}
\details{
Mutates \code{acc$entry_codes} and \code{acc$entry_segments} via
environment reference so the caller doesn't have to thread them
through return values.
}
\keyword{internal}
