% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metric_primitives.R
\name{metric_primitives}
\alias{metric_primitives}
\alias{prim_mean}
\alias{prim_median}
\alias{prim_mode_value}
\alias{prim_sd}
\alias{prim_mad}
\alias{prim_iqr}
\alias{prim_range_width}
\alias{prim_cv}
\alias{prim_min}
\alias{prim_max}
\alias{prim_quantile}
\alias{prim_p10}
\alias{prim_p25}
\alias{prim_p75}
\alias{prim_p90}
\alias{prim_p95}
\alias{prim_p99}
\alias{prim_n}
\alias{prim_n_unique}
\alias{prim_skewness}
\alias{prim_kurtosis_excess}
\alias{prim_log_mean}
\alias{prim_log_sd}
\alias{prim_outlier_count_iqr}
\alias{prim_max_to_median_ratio}
\alias{prim_proportion_zero}
\alias{prim_proportion_nonzero}
\alias{prim_proportion_above}
\alias{prim_proportion_below}
\alias{prim_proportion_in_range}
\alias{prim_entropy}
\alias{prim_frequency_distribution}
\alias{prim_top_k_values}
\alias{prim_top3_values}
\alias{prim_hour_of_day_distribution}
\alias{prim_day_of_week_distribution}
\alias{prim_month_of_year_distribution}
\alias{prim_time_span_days}
\alias{prim_median_seconds_between}
\alias{prim_entries_by_month}
\alias{prim_entries_over_time}
\alias{prim_circular_mean}
\alias{prim_circular_variance}
\alias{prim_peak_hour_circular}
\alias{prim_shapiro_p}
\title{Backend metric primitives (catalog functions)}
\usage{
prim_mean(x)

prim_median(x)

prim_mode_value(x)

prim_sd(x)

prim_mad(x)

prim_iqr(x)

prim_range_width(x)

prim_cv(x)

prim_min(x)

prim_max(x)

prim_quantile(x, q)

prim_p10(x)

prim_p25(x)

prim_p75(x)

prim_p90(x)

prim_p95(x)

prim_p99(x)

prim_n(x)

prim_n_unique(x)

prim_skewness(x)

prim_kurtosis_excess(x)

prim_log_mean(x)

prim_log_sd(x)

prim_outlier_count_iqr(x)

prim_max_to_median_ratio(x)

prim_proportion_zero(x)

prim_proportion_nonzero(x)

prim_proportion_above(x, threshold)

prim_proportion_below(x, threshold)

prim_proportion_in_range(x, lo, hi)

prim_entropy(x)

prim_frequency_distribution(x)

prim_top_k_values(x, k)

prim_top3_values(x)

prim_hour_of_day_distribution(t)

prim_day_of_week_distribution(t)

prim_month_of_year_distribution(t)

prim_time_span_days(t)

prim_median_seconds_between(t)

prim_entries_by_month(t)

prim_entries_over_time(t, bin_width_days)

prim_circular_mean(x)

prim_circular_variance(x)

prim_peak_hour_circular(t)

prim_shapiro_p(x)
}
\arguments{
\item{x}{Numeric vector (the metric column). Non-numeric values are coerced
and dropped.}

\item{q}{Quantile probability in [0, 1] (for \code{\link[=prim_quantile]{prim_quantile()}}).}

\item{threshold}{Numeric cut point (for the proportion-above/below
primitives).}

\item{lo, hi}{Inclusive numeric bounds (for \code{\link[=prim_proportion_in_range]{prim_proportion_in_range()}}).}

\item{k}{Positive integer: number of most-frequent values to return (for
\code{\link[=prim_top_k_values]{prim_top_k_values()}}).}

\item{t}{Timestamp vector: either POSIXct or epoch-seconds (numeric). Parsed
as UTC.}

\item{bin_width_days}{Positive numeric bin width in days (for
\code{\link[=prim_entries_over_time]{prim_entries_over_time()}}).}
}
\value{
A length-1 numeric for scalar primitives, or a named numeric vector
for distribution primitives.
}
\description{
A flat catalog of small, deterministic statistics the Methodology
Assistant can request by name (see \code{\link[=metric_catalog]{metric_catalog()}} and
\code{\link[=compute_metric_stat]{compute_metric_stat()}}). Each scalar primitive returns a length-1 numeric
(NA on empty / all-NA / insufficient input); each distribution primitive
returns a named numeric vector (empty named numeric on no data). All strip
NAs internally and never error on degenerate input.
}
\details{
These are backend scaffolding, not a user-facing API: researchers never pick
from this catalog. They are documented for maintainers and community
contributors extending the catalog.
}
\section{Functions}{
\itemize{
\item \code{prim_mean()}: Arithmetic mean. Honest for symmetric data;
misleading for skewed / heavy-tailed data (prefer \code{\link[=prim_median]{prim_median()}}).

\item \code{prim_median()}: Median (50th percentile). Robust to outliers
and skew; the honest center for heavy-tailed counts (e.g. upvotes).

\item \code{prim_mode_value()}: Statistical mode (most frequent value; ties
resolve to the smallest). Useful for discrete / coded / ordinal columns.

\item \code{prim_sd()}: Standard deviation (n-1 denominator). Pairs
with \code{\link[=prim_mean]{prim_mean()}}; like the mean, assumes a roughly symmetric scale.

\item \code{prim_mad()}: Median absolute deviation, normal-consistent
(constant 1.4826). The robust spread to pair with \code{\link[=prim_median]{prim_median()}}.

\item \code{prim_iqr()}: Interquartile range (Q3 - Q1, type-7). Robust
spread; the basis of the 1.5*IQR outlier rule.

\item \code{prim_range_width()}: Range width (max - min). Coarse spread; very
sensitive to single extreme values.

\item \code{prim_cv()}: Coefficient of variation (sd / mean). Unitless
relative dispersion; NA when the mean is ~0 (ratio undefined).

\item \code{prim_min()}: Minimum observed value.

\item \code{prim_max()}: Maximum observed value (the single most extreme
point; a heavy-tail signal).

\item \code{prim_quantile()}: Arbitrary quantile at probability \code{q} (type-7).
Parameterized variant used mainly by the pinned-replay path; the live
schema offers the fixed-probability \code{prim_pNN} variants below.

\item \code{prim_p10()}: 10th percentile.

\item \code{prim_p25()}: 25th percentile (lower quartile, Q1).

\item \code{prim_p75()}: 75th percentile (upper quartile, Q3).

\item \code{prim_p90()}: 90th percentile. Honest "high end" for
heavy-tailed data where the mean is dominated by the tail.

\item \code{prim_p95()}: 95th percentile.

\item \code{prim_p99()}: 99th percentile (extreme tail).

\item \code{prim_n()}: Count of non-missing observations the statistic
rests on. (The dispatcher also reports this as \code{n_observed}.)

\item \code{prim_n_unique()}: Number of distinct non-missing values. Low
relative to n suggests a discrete / categorical column.

\item \code{prim_skewness()}: Skewness (moment coefficient g1 =
m3 / m2^1.5, the same convention as moments::skewness). >0 right-skewed,
<0 left-skewed, ~0 symmetric. NA for fewer than 3 observations or zero
variance.

\item \code{prim_kurtosis_excess()}: Excess kurtosis (g2 = m4 / m2^2 - 3), so a
normal distribution scores 0 and heavier-than-normal tails score >0. NA for
fewer than 4 observations or zero variance.

\item \code{prim_log_mean()}: Mean of log1p(x). A tail-dampened center for
right-skewed non-negative counts. NA if any value is negative (log1p
undefined there) -- a fail-honest signal that the column is not a
non-negative count.

\item \code{prim_log_sd()}: Standard deviation of log1p(x). Tail-dampened
spread for non-negative counts; NA if any value is negative.

\item \code{prim_outlier_count_iqr()}: Count of values beyond the 1.5*IQR Tukey
fences. A direct count of how many outliers a heavy tail produces.

\item \code{prim_max_to_median_ratio()}: Ratio of max to median. A scale-free heavy-tail
index: 1 means no tail, large values mean the maximum dwarfs the typical
case. NA when the median is ~0.

\item \code{prim_proportion_zero()}: Proportion of values exactly equal to 0. For
zero-inflated counts (e.g. how many entries received no upvotes).

\item \code{prim_proportion_nonzero()}: Proportion of values not equal to 0
(1 - proportion_zero).

\item \code{prim_proportion_above()}: Proportion of values strictly above
\code{threshold}. NA if \code{threshold} is missing.

\item \code{prim_proportion_below()}: Proportion of values strictly below
\code{threshold}. NA if \code{threshold} is missing.

\item \code{prim_proportion_in_range()}: Proportion of values in the inclusive range
[lo, hi]. NA if either bound is missing.

\item \code{prim_entropy()}: Shannon entropy in bits
(-sum p*log2 p over distinct values). 0 = one value only; log2(k) = uniform
over k values. A concentration / diversity measure for coded columns.

\item \code{prim_frequency_distribution()}: Full frequency distribution: a named numeric
vector of counts per distinct value (names are the values). Distribution
primitive.

\item \code{prim_top_k_values()}: The \code{k} most frequent values and their counts
(named numeric vector, descending). Distribution primitive.

\item \code{prim_top3_values()}: The 3 most frequent values and their counts
(zero-arg convenience over \code{\link[=prim_top_k_values]{prim_top_k_values()}}). Distribution primitive.

\item \code{prim_hour_of_day_distribution()}: Counts of entries per clock hour (0-23, UTC).
Distribution primitive; reveals time-of-day rhythm.

\item \code{prim_day_of_week_distribution()}: Counts of entries per ISO weekday
(Mon..Sun, locale-independent). Distribution primitive.

\item \code{prim_month_of_year_distribution()}: Counts of entries per calendar month
(Jan..Dec). Distribution primitive; reveals seasonality.

\item \code{prim_time_span_days()}: Span in days between the earliest and latest
timestamp. NA for fewer than 2 timestamps.

\item \code{prim_median_seconds_between()}: Median gap, in seconds, between consecutive
timestamps (sorted). Posting cadence; NA for fewer than 2 timestamps.

\item \code{prim_entries_by_month()}: Counts of entries per calendar month
(YYYY-MM, chronological). Distribution primitive; the volume timeline.

\item \code{prim_entries_over_time()}: Counts of entries in fixed-width time bins of
\code{bin_width_days} days, measured from the earliest timestamp. Distribution
primitive (bin start dates as names). NA-shaped when \code{bin_width_days} is
missing/invalid.

\item \code{prim_circular_mean()}: Circular (directional) mean of angles in
radians, normalized to [0, 2*pi). Implemented as
atan2(mean sin, mean cos). Use for periodic quantities where a plain mean
is wrong (e.g. averaging 23:00 and 01:00 should give ~midnight, not noon).

\item \code{prim_circular_variance()}: Circular variance of angles in radians: 1 - R,
where R is the mean resultant length. 0 = perfectly concentrated, 1 =
uniformly dispersed around the circle.

\item \code{prim_peak_hour_circular()}: Peak (mean) hour of day on a 24h clock, in
[0, 24). Maps each timestamp's time-of-day to an angle, takes the circular
mean, and converts back to clock hours -- the methodologically correct
"typical time of day", unlike a linear average of hour numbers.

\item \code{prim_shapiro_p()}: Shapiro-Wilk normality test p-value. Small p
(e.g. <0.05) indicates non-normality, justifying robust/rank methods. NA
for fewer than 3 observations or fewer than 3 distinct values. For n > 5000
a seeded 5000-point subsample is tested (Shapiro-Wilk's supported range).

}}
\keyword{internal}
