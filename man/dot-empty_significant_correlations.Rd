% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/14_correlations.R
\name{.empty_significant_correlations}
\alias{.empty_significant_correlations}
\title{Empty, full-schema significant-correlations tibble}
\usage{
.empty_significant_correlations(include_method = FALSE)
}
\arguments{
\item{include_method}{Logical; whether to include the per-pair \code{method}
column (present only when the caller computed per-pair methods).}
}
\value{
A 0-row tibble with the full extract_significant column set.
}
\description{
Returned by \code{\link[=extract_significant]{extract_significant()}} when no pairwise correlation was
computable (every off-diagonal r was NA). Carries every column the
populated path emits -- including the \code{exclusion_reason}
and \code{excluded_from_findings} flags -- so the CSV export, the report
consumers, and compare_runs see a consistent schema on degenerate corpora.
}
\keyword{internal}
