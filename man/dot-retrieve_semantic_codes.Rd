% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/09_coding.R
\name{.retrieve_semantic_codes}
\alias{.retrieve_semantic_codes}
\title{Phase 58 Tier 0 C-6: per-entry semantic top-K retrieval against codebook}
\usage{
.retrieve_semantic_codes(state, code_data, entry_text, provider, top_k)
}
\arguments{
\item{state}{ProgressiveCodingState carrying $codebook + $semantic_cache.}

\item{code_data}{List of per-code records (key/name/desc/freq/type),
matching the same row-order indices used by the caller.}

\item{entry_text}{Current entry's raw text (single character scalar).}

\item{provider}{AIProvider used to compute embeddings.}

\item{top_k}{Maximum number of semantic-retrieval indices to return.}
}
\value{
list(indices = integer, new_embeddings = named list of vectors)
}
\description{
Computes cosine similarity between the current entry's embedding and
each code's \code{name: description} embedding, returning the top-K indices.
Uses + populates \code{state$semantic_cache$code_embeddings} so each code is
embedded at most once across the full coding run. Returns
\code{integer(0)} when the provider doesn't support embeddings, when no
provider/entry_text is supplied, or when the API call fails. Always
degrades gracefully -- the caller falls back to frequency-only.
}
\keyword{internal}
