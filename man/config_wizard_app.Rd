% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/20_shiny_wizard.R
\name{config_wizard_app}
\alias{config_wizard_app}
\title{Launch the interactive configuration wizard}
\usage{
config_wizard_app(output_path = "config.yaml", .return_app = FALSE)
}
\arguments{
\item{output_path}{Where to save the generated config (default "config.yaml").
The user can also change this in the UI.}

\item{.return_app}{Internal testing hook. When \code{TRUE}, returns the Shiny
app object (for \code{shiny::testServer}) instead of launching it.}
}
\value{
The path to the created config file (invisibly). Returns NULL if the
user closes the app without saving.
}
\description{
Opens a Shiny app that walks you through every configuration option
with descriptions and sensible defaults. The step gates enforce the fields
\code{\link[=validate_config]{validate_config()}} requires (an explicit methodology mode, a research
focus, a data path, and a framework spec for Mode 3), so the \code{config.yaml}
it writes loads cleanly.
}
\details{
This is the web-based companion to the CLI-based \code{\link[=config_wizard]{config_wizard()}}. Both
write an equivalent \code{config.yaml} (the same required sections and methodology
block); exact field defaults may differ between the two entry points.
}
