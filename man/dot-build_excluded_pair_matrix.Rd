% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/14_correlations.R
\name{.build_excluded_pair_matrix}
\alias{.build_excluded_pair_matrix}
\title{Build an index-aligned logical matrix of excluded (circular) pairs}
\usage{
.build_excluded_pair_matrix(var_names, excluded_pairs)
}
\arguments{
\item{var_names}{Character vector of the correlation matrix's variables
(original, un-humanized names).}

\item{excluded_pairs}{The extract_significant() data frame, or NULL.}
}
\value{
An \verb{n x n} logical matrix.
}
\description{
Given the plot's variable names and the \code{\link[=extract_significant]{extract_significant()}} result,
returns an \verb{n x n} logical matrix that is TRUE at \verb{[i, j]} (and \verb{[j, i]})
whenever the unordered pair \verb{(var_names[i], var_names[j])} was flagged
\code{excluded_from_findings}. The matrix is index-aligned to the correlation
matrix, so it survives the plot's variable-name humanizing. Returns an
all-FALSE matrix for NULL / empty / malformed input (back-compat no-op).
}
\keyword{internal}
