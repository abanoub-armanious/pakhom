% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/14_correlations.R
\name{.mask_excluded_pvalues}
\alias{.mask_excluded_pvalues}
\title{Mask excluded pairs' adjusted p-values to non-significant}
\usage{
.mask_excluded_pvalues(pa, excluded_mat)
}
\arguments{
\item{pa}{Adjusted-p matrix aligned to the correlation matrix (or NULL).}

\item{excluded_mat}{Logical matrix from \code{\link[=.build_excluded_pair_matrix]{.build_excluded_pair_matrix()}}.}
}
\value{
The (possibly masked) p-matrix.
}
\description{
Sets the p-value of every excluded (circular) pair to 1 so corrplot's
\code{insig = "blank"} wipes its glyph -- the pair is not shown as a
significant finding in the heatmap (it remains in correlations.csv with
its real values). NULL \code{pa} or no exclusions returns \code{pa} unchanged.
}
\keyword{internal}
