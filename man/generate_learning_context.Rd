% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_manuscript_learning.R
\name{generate_learning_context}
\alias{generate_learning_context}
\title{Generate task-specific learning context from previous analyses}
\usage{
generate_learning_context(
  studies,
  max_codebook_chars = 20000L,
  max_manuscript_chars = 12000L,
  max_raw_samples = 5L,
  seed = 42L
)
}
\arguments{
\item{studies}{PreviousStudies object}

\item{max_codebook_chars}{Max characters for codebook context per study}

\item{max_manuscript_chars}{Max characters for manuscript supplements per study}

\item{max_raw_samples}{Max raw data examples per study}

\item{seed}{Single RNG seed used for ALL example sampling, so the learning
context is reproducible across runs (default 42).}
}
\value{
LearningContext S3 object
}
\description{
Produces a LearningContext object with separate slices for coding,
theming, and review prompts. Uses a CODEBOOK-FIRST approach: the
codebook hierarchy (themes, subthemes, codes, descriptions, frequencies,
and entry-level coding examples) is the primary learning source.
Manuscripts are supplementary, used only when codebook descriptions
are lacking.
}
