% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provocateur.R
\name{run_provocateur_questioning}
\alias{run_provocateur_questioning}
\title{Run provocateur questioning across themes (Mode 1 entry point)}
\usage{
run_provocateur_questioning(
  data,
  theme_set,
  provider,
  config = list(),
  categories = .VALID_PROVOCATION_CATEGORIES,
  audit_log = NULL,
  response_cache = NULL,
  fabrication_log = NULL,
  resume_log = NULL
)
}
\arguments{
\item{data}{Tibble with std_id + std_text (standardized corpus).}

\item{theme_set}{ThemeSet object. Each theme drives one round of
provocations. The researcher must have authored these themes; the
provocateur does NOT name themes.}

\item{provider}{AIProvider object.}

\item{config}{Optional config list (used for logging/context).}

\item{categories}{Character vector of category names to run (default:
all five).}

\item{audit_log}{Optional AuditLog from \code{init_audit_log}.}

\item{response_cache}{Optional ResponseCache.}

\item{fabrication_log}{Optional FabricationLog.}

\item{resume_log}{Optional \code{ResearcherReflectionLog} to append
to (resume semantics).}
}
\value{
A \code{ResearcherReflectionLog} with provocations populated.
}
\description{
Orchestrates the five (or selected) provocation categories per theme,
assembling a \code{ResearcherReflectionLog} that captures every
provocation issued. The log is the AI's contribution to a Mode 1
analysis -- the THEMES + CODES are the researcher's authorship, kept
on the same log object as \code{researcher_authored_codes /
researcher_authored_themes}.
}
\details{
Per AC7 (universal Tier-0): every provocation that cites verbatim
evidence runs through \code{verify_quote}; fabricated provocations
are dropped silently and recorded to the audit + fabrication logs.
}
