% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_state.R
\name{init_run_state}
\alias{init_run_state}
\title{Initialize the run-metadata record for a new (or resumed) run}
\usage{
init_run_state(
  run_dir,
  run_id,
  methodology_mode,
  parent_run_id = NULL,
  mode_changed_from = NULL,
  ...
)
}
\arguments{
\item{run_dir}{Path to the run directory.}

\item{run_id}{Stable run identifier (often the basename of \code{run_dir}).}

\item{methodology_mode}{Character: the declared mode for this run.}

\item{parent_run_id}{Optional character: when this run is a fork of a
prior run with a different methodology, the parent's run_id.}

\item{mode_changed_from}{Optional character: the methodology mode of
the parent run (when forked). NULL when this is a fresh run.}

\item{...}{Additional fields written verbatim into run_metadata.json
(e.g., \code{provider = "anthropic"}, \code{study_name = "..."}).}
}
\value{
The metadata list (existing or newly written).
}
\description{
Builds the run_metadata.json content from the supplied run details and
writes it to \code{run_dir}. Idempotent on resume: when
\code{run_metadata.json} already exists, returns the existing metadata
WITHOUT overwriting (so finalized markers / parent_run_id aren't
clobbered by a casual re-init). To recreate, delete the file first.
}
