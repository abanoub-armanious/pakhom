% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/14_correlations.R
\name{.create_correlation_lollipop}
\alias{.create_correlation_lollipop}
\title{Top-N effect-size lollipop chart for large correlation matrices}
\usage{
.create_correlation_lollipop(
  cm,
  pa,
  output_path,
  top_n,
  n_total_vars,
  methodology_mode = NULL,
  run_id = NULL,
  excluded_mat = NULL
)
}
\arguments{
\item{cm}{Correlation matrix (rownames and colnames already humanized
by the caller).}

\item{pa}{Adjusted-p matrix aligned to \code{cm}; NAs treated as
non-significant.}

\item{output_path}{File path for PNG output.}

\item{top_n}{Integer; number of top pairs to show.}

\item{n_total_vars}{Integer; total variables in the underlying
matrix (used in the subtitle to make the filter explicit).}

\item{methodology_mode}{AC4 caption.}

\item{run_id}{AC4 caption.}

\item{excluded_mat}{Logical matrix from \code{\link[=.build_excluded_pair_matrix]{.build_excluded_pair_matrix()}}
marking analyst-internal / circular pairs (or NULL). Such pairs are
labelled "excluded (circular)" instead of being shown as a finding.}
}
\description{
Fallback: when the variable count exceeds the
heatmap legibility threshold (\code{max_inline_vars}), render the
top-N unique pairs by \code{|r|} as a horizontal lollipop. Pairs
are extracted from the upper triangle of the correlation matrix
(each pair appears once). Significance, when available, is encoded
by point color (Bonferroni-adjusted \code{p < 0.05} vs not).
}
\keyword{internal}
