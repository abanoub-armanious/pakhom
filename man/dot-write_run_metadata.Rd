% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_state.R
\name{.write_run_metadata}
\alias{.write_run_metadata}
\title{Write run metadata for a run directory}
\usage{
.write_run_metadata(run_dir, metadata)
}
\arguments{
\item{run_dir}{Path to a run output directory (must exist).}

\item{metadata}{Named list to serialize.}
}
\value{
TRUE on success, FALSE on failure (warning logged).
}
\description{
Writes \code{metadata} to \code{run_metadata.json} in JSON form.
Atomic-ish: writes to a temp file first, then renames. If the
atomic-rename fails (e.g., cross-filesystem move), falls back to a
direct write -- the failure modes are not silent because both branches
log on error.
}
\keyword{internal}
