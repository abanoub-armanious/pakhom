% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/memos.R
\name{load_memos}
\alias{load_memos}
\title{Load memos from a run output directory back into Memo objects}
\usage{
load_memos(run_dir)
}
\arguments{
\item{run_dir}{Path to the run output directory.}
}
\value{
List of \code{Memo} objects (zero-length when no memos
exist or the memos directory is missing).
}
\description{
Reads every \code{memos/*.md} file in \code{run_dir} and parses it
via \code{\link{markdown_to_memo}}. Used by \code{run_mode1()} on
the resume path so a previously persisted memo set survives across
interrupted runs.
}
