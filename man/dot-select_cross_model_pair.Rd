% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/15_comparison.R
\name{.select_cross_model_pair}
\alias{.select_cross_model_pair}
\title{Choose a cross-model pair from discovered run directories}
\usage{
.select_cross_model_pair(all_dirs, models_used)
}
\arguments{
\item{all_dirs}{Character vector of run directories in chronological
(oldest-first) order, as returned by \code{.discover_run_dirs}.}

\item{models_used}{Named list mapping \code{basename(dir)} to a
\code{"provider/model"} key.}
}
\value{
A list with \code{current} (newest dir), \code{partner} (newest
different-model dir, or \code{NULL} if none), and the two model keys.
}
\description{
Returns the newest run and the newest run that used a DIFFERENT model, so
\code{\link{compare_models}} compares two distinct models rather than the
two chronologically-latest runs (which could be the same model).
}
\keyword{internal}
