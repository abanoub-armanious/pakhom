# Determine the last completed step for resume

Determine the last completed step for resume

## Usage

``` r
find_resume_point(manager)
```

## Arguments

- manager:

  CheckpointManager object

## Value

Character: last completed step name, or NULL if no checkpoints
