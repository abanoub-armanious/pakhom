# Distance summary for a cluster (always shows the most-distant pair)

Distance summary for a cluster (always shows the most-distant pair)

## Usage

``` r
.cluster_distance_summary(cluster_leaves, codes, distance_matrix)
```
