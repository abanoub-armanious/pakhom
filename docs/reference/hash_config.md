# Compute a hash of a config file for change detection

Compute a hash of a config file for change detection

## Usage

``` r
hash_config(config_path)
```

## Arguments

- config_path:

  Path to YAML config file

## Value

Character hash string
