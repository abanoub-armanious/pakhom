# Valid verification methods (which ladder step matched)

Valid verification methods (which ladder step matched)

## Usage

``` r
.VALID_QUOTE_VERIFICATION_METHODS
```

## Format

An object of class `character` of length 6.
