# Build a QuoteProvenance for the schema path (offsets from the model)

Build a QuoteProvenance for the schema path (offsets from the model)

## Usage

``` r
.build_quote_from_schema_path(
  seg_text,
  seg_start,
  seg_end,
  text,
  entry_id,
  code_key,
  ai_meta
)
```
