# Schema for an array of extractive citations (entry_id + span + reason)

Used by counter_narrative, disconfirming_evidence,
alternative_interpretation (each citation in the supporting evidence).

## Usage

``` r
.provocation_citation_schema()
```
