# Render a single provocation as an HTML block (called from the per-theme section)

Render a single provocation as an HTML block (called from the per-theme
section)

## Usage

``` r
.render_provocation_block(p)
```
