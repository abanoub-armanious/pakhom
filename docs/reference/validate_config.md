# Validate configuration completeness and correctness

Validate configuration completeness and correctness

## Usage

``` r
validate_config(config)
```

## Arguments

- config:

  A ThematicConfig object

## Value

The config invisibly, or stops with informative errors
