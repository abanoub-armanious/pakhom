# Schema for alternative_interpretation provocations

Returns alternative theme names + the same supporting quotes; no "which
is better" judgment.

## Usage

``` r
.coding_schema_alternative_interpretation()
```
