# Compare analytical samples across runs

Compare analytical samples across runs

## Usage

``` r
.compare_samples(snapshots, current)
```

## Arguments

- snapshots:

  List of RunSnapshot objects (chronological)

- current:

  RunSnapshot for the current run

## Value

List with per_run, pairwise, text_changes, interpretation
