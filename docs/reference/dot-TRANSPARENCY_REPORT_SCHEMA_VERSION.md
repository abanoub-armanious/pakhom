# Schema version for the transparency report

- 1.0.0 (Phase 58 / Stage 1A): initial schema. Sections: run_metadata,
  lincoln_guba, reflexivity, quote_provenance, corpus_coverage,
  audit_summary, theme_set.

## Usage

``` r
.TRANSPARENCY_REPORT_SCHEMA_VERSION
```

## Format

An object of class `character` of length 1.
