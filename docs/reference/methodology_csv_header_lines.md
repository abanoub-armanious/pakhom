# Generate the comment-header lines (without writing) for a CSV stamp

Useful for callers that build a CSV in-memory and want to prepend the
stamp before writing.

## Usage

``` r
methodology_csv_header_lines(mode, run_id = NULL)
```
