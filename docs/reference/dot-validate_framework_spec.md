# Validate the parsed raw framework spec and build a FrameworkSpec object

Validate the parsed raw framework spec and build a FrameworkSpec object

## Usage

``` r
.validate_framework_spec(raw, source_path = NA_character_)
```
