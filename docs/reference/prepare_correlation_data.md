# Prepare data for correlation analysis

Prepare data for correlation analysis

## Usage

``` r
prepare_correlation_data(data, theme_set, config = list())
```

## Arguments

- data:

  tibble with theme assignments and sentiment columns

- theme_set:

  ThemeSet object

- config:

  Correlation config section

## Value

tibble of numeric columns ready for correlation
