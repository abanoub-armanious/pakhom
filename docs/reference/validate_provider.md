# Validate that an AI provider is properly constructed

Validate that an AI provider is properly constructed

## Usage

``` r
validate_provider(provider, allow_null = FALSE, caller = "unknown")
```

## Arguments

- provider:

  Object to validate

- allow_null:

  If TRUE, NULL is accepted (for optional provider)

- caller:

  Name of the calling function
