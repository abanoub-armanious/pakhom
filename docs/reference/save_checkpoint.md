# Save checkpoint at a given pipeline step

Save checkpoint at a given pipeline step

## Usage

``` r
save_checkpoint(manager, step_name, data)
```

## Arguments

- manager:

  CheckpointManager object

- step_name:

  Step identifier (e.g., "sentiment_done")

- data:

  The data/results to save
