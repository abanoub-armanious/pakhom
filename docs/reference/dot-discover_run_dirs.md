# Find all timestamped run directories in chronological order

Find all timestamped run directories in chronological order

## Usage

``` r
.discover_run_dirs(results_base)
```

## Arguments

- results_base:

  Base results directory

## Value

Character vector of full paths, oldest first
