# One-line description of what the mode commits the AI to

Used in HTML stamps as a tooltip / caption. Mirrors the per-mode rule
blocks in `R/methodology_rules.R` but in a one-liner form.

## Usage

``` r
methodology_description_short(mode)
```

## Arguments

- mode:

  Character.

## Value

Character description.
