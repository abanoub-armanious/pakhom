# List the built-in frameworks shipped with pakhom

Returns a character vector of built-in framework aliases. Each alias
resolves via
[`load_framework_spec`](https://abanoub-armanious.github.io/pakhom/reference/load_framework_spec.md).

## Usage

``` r
list_builtin_frameworks()
```

## Value

Character vector of alias names.
