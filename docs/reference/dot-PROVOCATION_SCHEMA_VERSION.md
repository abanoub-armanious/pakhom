# Schema version for individual Provocation S3 objects.

Tracks the structure of a single provocation record (separate from the
parent ResearcherReflectionLog version). Bumped when the Provocation
schema changes incompatibly.

## Usage

``` r
.PROVOCATION_SCHEMA_VERSION
```

## Format

An object of class `character` of length 1.
