# Resolve relative file/directory paths relative to the config file location

Resolve relative file/directory paths relative to the config file
location

## Usage

``` r
.resolve_paths(config, config_dir)
```

## Arguments

- config:

  Config list

- config_dir:

  Absolute path to the directory containing config.yaml

## Value

Config with resolved absolute paths
