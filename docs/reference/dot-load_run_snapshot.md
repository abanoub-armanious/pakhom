# Load a single run's artifacts from disk

Load a single run's artifacts from disk

## Usage

``` r
.load_run_snapshot(run_dir)
```

## Arguments

- run_dir:

  Path to one run folder

## Value

A RunSnapshot list
