# Valid citation sources (where the quote came from)

Valid citation sources (where the quote came from)

## Usage

``` r
.VALID_QUOTE_CITATION_SOURCES
```

## Format

An object of class `character` of length 4.
