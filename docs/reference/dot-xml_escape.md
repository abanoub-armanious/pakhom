# Escape a string for safe inclusion as XML text content

Escape a string for safe inclusion as XML text content

## Usage

``` r
.xml_escape(x)
```

## Arguments

- x:

  Character string

## Value

Escaped character string
