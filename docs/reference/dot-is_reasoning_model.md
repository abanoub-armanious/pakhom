# Check if a model is a reasoning model (o1/o3/o4 series) These need different API parameters (no temperature, max_completion_tokens)

Check if a model is a reasoning model (o1/o3/o4 series) These need
different API parameters (no temperature, max_completion_tokens)

## Usage

``` r
.is_reasoning_model(model)
```

## Arguments

- model:

  Model name string
