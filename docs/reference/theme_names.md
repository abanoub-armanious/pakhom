# Extract theme names from ThemeSet

Extract theme names from ThemeSet

## Usage

``` r
theme_names(theme_set)
```

## Arguments

- theme_set:

  ThemeSet object

## Value

Character vector of theme names
